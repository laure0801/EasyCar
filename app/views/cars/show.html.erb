<h1><%= @car.brand %> <%= @car.model %> </h1>
  <%= cl_image_tag @car.photo.key, height: 300, width: 400, crop: :fill %>
 <p> <%= @car.description %></p>
 <%= link_to "Réserver cette voiture", new_car_booking_path(@car) %>
